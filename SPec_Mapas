{
  "$schema": "https://vega.github.io/schema/vega/v5.json",
  "description": "Mapa técnico con imagen dinámica y botones",
  "autosize": {
    "type": "fit",
    "contains": "padding"
  },
  "padding": 0,
  "width": 900,
  "height": 550,

  "signals": [
    { "name": "defaultBtnWidth", "value": 120 },
    { "name": "defaultBtnHeight", "value": 30 },

    {
      "name": "imgWidth",
      "update": "data('mapa').length ? width : width"
    },
    {
      "name": "imgHeight",
      "update": "data('mapa').length ? height : height"
    },
    {
      "name": "imgX",
      "value": 0
    },
    {
      "name": "imgY",
      "value": 0
    }
  ],

  "data": [
    {
      "name": "dataset"
    },
    {
      "name": "mapa",
      "source": "dataset",
      "transform": [
        {
          "type": "filter",
          "expr": "isValid(datum.Img64) && datum.Img64 != ''"
        },
        {
          "type": "limit",
          "limit": 1
        }
      ]
    },
    {
      "name": "botones",
      "source": "dataset",
      "transform": [
        {
          "type": "filter",
          "expr": "isValid(datum.Boton) && datum.Boton != '' && isValid(datum.labelx) && isValid(datum.labely) && isValid(datum.pointx) && isValid(datum.pointy)"
        },
        {
          "type": "collect",
          "sort": {
            "field": "order",
            "order": "ascending"
          }
        }
      ]
    }
  ],

  "marks": [
    {
      "type": "image",
      "from": { "data": "mapa" },
      "encode": {
        "enter": {
          "x": { "value": 0 },
          "y": { "value": 0 },
          "width": { "signal": "width" },
          "height": { "signal": "height" },
          "url": { "field": "Img64" }
        }
      }
    },

    {
      "type": "rule",
      "from": { "data": "botones" },
      "encode": {
        "enter": {
          "x": { "signal": "datum.labelx * width + defaultBtnWidth / 2" },
          "y": { "signal": "datum.labely * height + defaultBtnHeight / 2" },
          "x2": { "signal": "datum.pointx * width" },
          "y2": { "signal": "datum.pointy * height" },
          "stroke": { "value": "#49B3FF" },
          "strokeWidth": { "value": 2 }
        }
      }
    },

    {
      "type": "symbol",
      "from": { "data": "botones" },
      "encode": {
        "enter": {
          "x": { "signal": "datum.pointx * width" },
          "y": { "signal": "datum.pointy * height" },
          "size": { "value": 80 },
          "fill": { "value": "#A6ECFF" },
          "stroke": { "value": "#49B3FF" },
          "strokeWidth": { "value": 2 }
        }
      }
    },

    {
      "type": "rect",
      "from": { "data": "botones" },
      "encode": {
        "enter": {
          "x": { "signal": "datum.labelx * width" },
          "y": { "signal": "datum.labely * height" },
          "width": { "signal": "defaultBtnWidth" },
          "height": { "signal": "defaultBtnHeight" },
          "fill": {
            "signal": "isValid(datum.color) && datum.color != '' ? datum.color : '#2F80ED'"
          },
          "cornerRadius": { "value": 14 },
          "stroke": { "value": "#72D1FF" },
          "strokeWidth": { "value": 1.5 }
        },
        "update": {
          "tooltip": {
            "signal": "{'Botón': datum.Boton, 'Detalle': datum.tooltip, 'Mapa': datum.nombreMapa}"
          }
        }
      }
    },

    {
      "type": "text",
      "from": { "data": "botones" },
      "encode": {
        "enter": {
          "x": { "signal": "datum.labelx * width + defaultBtnWidth / 2" },
          "y": { "signal": "datum.labely * height + defaultBtnHeight / 2" },
          "text": { "field": "Boton" },
          "align": { "value": "center" },
          "baseline": { "value": "middle" },
          "fill": { "value": "white" },
          "fontSize": { "value": 12 },
          "fontWeight": { "value": "bold" }
        }
      }
    }
  ]
}
